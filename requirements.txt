# Core LangChain — pinned to working compatible set
langchain==0.3.0
langchain-core==0.3.0
langchain-community==0.3.0
langchain-openai==0.2.0        # also used for Gemini via OpenAI-compatible endpoint

# Vector Store & Embeddings
langchain-chroma==0.1.4
langchain-huggingface==0.1.0
chromadb==0.5.3
sentence-transformers>=3.0.0

# LangGraph — pinned checkpoint/sdk to match langgraph 0.2.28
langgraph==0.2.28
langgraph-checkpoint==1.0.12
langgraph-sdk==0.1.42

# Reranking
# NOTE: 0.2.4/0.2.5/0.2.6 hard-depend on llama-cpp-python, which needs a C++
# compiler + CMake to build from source and has no prebuilt wheel for many
# Windows/Python combos ("CMake configuration failed" during pip install).
# 0.2.7+ made llama-cpp-python an optional 'listwise' extra — the default
# cross-encoder model used in rag_service.py (ms-marco-MiniLM-L-12-v2) never
# touches that code path, so plain `pip install flashrank` no longer needs a
# C++ toolchain at all. Same Ranker/RerankRequest API — no code changes needed.
flashrank==0.2.7

# PDF Processing
pypdf>=3.0.0
langchain-text-splitters==0.3.0

# Tools
duckduckgo-search>=6.0.0

# CLI & Server
click>=8.1.0
rich>=13.0.0
python-dotenv>=1.0.0
fastapi>=0.110.0
uvicorn>=0.29.0
python-multipart>=0.0.9

# Utilities
pydantic>=2.7.0
numpy>=1.24.0,<2          # NumPy 2.x breaks chromadb 0.4.x / 0.5.x
tenacity>=8.1.0,<9        # langchain-community requires <9
msgpack>=1.0.0            # required by langgraph-checkpoint
requests>=2.31.0
PyYAML>=6.0.0
